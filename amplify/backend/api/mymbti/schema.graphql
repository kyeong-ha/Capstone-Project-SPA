type MBTI @model {
    id: ID!
    mbti: String!
    mind: String
    type: String
}

type Input @model{
    id: ID!
    question: String
    answer: String
}

type InputResult {
    isSucceeded: Boolean!
}

input InputArgs {
    id: ID!
    question: String
    answer: String
}